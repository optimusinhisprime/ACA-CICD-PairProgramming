version: 2.1

workflows:
  test-deploy:
    jobs:
      - test

jobs:
  test:         
    docker:
      - image: cimg/node:17.5.0
    steps:
      - checkout
      - run: 
          name: install dependencies
          command: npm install
      - run:
          name: Run tests
          command: npm test
      